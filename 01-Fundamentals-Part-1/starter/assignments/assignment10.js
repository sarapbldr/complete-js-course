let myPopulation = 46940000;
const myCountry = 'Spain';

let averagePopulation = 33000000;

console.log(
  myPopulation > averagePopulation
    ? `${myCountry}'s population is above average`
    : `${myCountry}'s population is below average`
);
